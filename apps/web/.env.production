# Public frontend URL for production builds.
NEXT_PUBLIC_SITE_URL="https://rukhsar-web.vercel.app"

# Set this in Vercel to the deployed Express API base URL when the API is deployed.
# Do not use localhost here, because it is baked into the browser bundle.
NEXT_PUBLIC_API_URL=""

NEXT_PUBLIC_RAZORPAY_KEY_ID=""
